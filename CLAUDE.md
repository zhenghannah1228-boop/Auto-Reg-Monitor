# 项目移交:汽车法规库监控台 · 二期(服务器化)开发

> 一期 `archive/phase1/index.html` 是全部业务逻辑的唯一事实来源(source of truth),
> 二期是**移植**它,不是重新发明。

---

## 一、项目背景

这是一个汽车行业出口法规库的监控工具,覆盖 60+ 目标国家、10 个法规维度。
一期是纯前端单文件 HTML(所有 JS/CSS 内联),用户在浏览器本地载入 Excel 清单
和法规原文文件,完成监控、对账、关系分析。

**二期目标:部署为服务器网页,实现数据持久化与团队共享** —— 任何同事打开网页
即看到同一份持续维护的法规库,不再每次手动载入文件。

## 二、现有资产(必读)

| 文件 | 说明 |
|---|---|
| `archive/phase1/index.html` | 一期完整实现,所有业务逻辑内联在最后一个 `<script>` 块 |
| `xlsx.full.min.js` | SheetJS 0.18.5,已本地化,前端 Excel 解析 |

一期功能(7 个标签页,二期须全部保留):
1. **总览排行** — KPI(总条数/覆盖国家/本次新增/近两年新法规/空白超标国家)+ 各国法规量排行(附 10 维度迷你色块)
2. **维度矩阵** — 国家 × 10 维度热力表,首行首列冻结
3. **国家详情** — 单国 10 维度分布、最新修订列表(日期倒序+修订系列标签)、国际标准引用排行
4. **新增监控** — 与历史基准对比,按国家列出新增/移除
5. **关系网络** — Canvas 力导向图,两种模式:跨国联动(国家↔国际标准枢纽)/ 单国内部(法规按维度着色,含上下位箭头边)
6. **层级树** — 基于「上位法规」列的可折叠树(框架法规→技术法规→细则)
7. **文本对账** — 法规清单 ↔ 法规原文文件 双向对账,导出三 sheet Excel
另有:全局搜索(顶栏,`/` 聚焦,搜国家/法规/国际标准三类并跳转定位)。

## 三、必须原样保留的业务规则(最高优先级)

以下规则是与用户反复确认的结果,改动时**逐条对照一期源码**,不得擅自"优化":

### 3.1 输入数据格式(两种,自动识别,按列名精确匹配)
- **8 列国家清单**:国家 / 法规编号 / 法规中文名 / 法规原文名 / 语言 / 对标维度 / 链接 / 数据来源
- **28 列提取文件**:来源文档名称、序号、区域、国家、法规编号、法规中文名称、法规英文名称、法规原文名称、法规类型、法规类别、法规性质、法规领域、管理机构、有无原文、有无中文译文、有无英文译文、发布日期、生效日期、新认证车具体生效日期、在产车具体生效日期、对应国标、对应欧标、对应美标、适用车型类型、适用动力类型、备注、原文语言、正式法规 or 草案
- **可选「上位法规」列**(任一格式均可追加):填上位法规编号(无编号填准确中文名),多个用半角分号 `;` 分隔(兼容 `；、,，`)
- 列名兼容:`法规中文名`/`法规中文名称`、`法规原文名`/`法规原文名称`、`语言`/`原文语言` 等同义列名都要认
- 解析 Excel 时**跳过** sheet 名含「维度涵盖 / 报告 / 说明」的工作表
- 一行有效的最低条件:有国家,且(法规编号或中文名)非空

### 3.2 十维度框架与归集逻辑(三层,顺序不可变)
维度:1 整车准入 / 2 环保全链条 / 3 射频认证 / 4 充电电池 / 5 EV补贴 / 6 网络安全 / 7 ADAS / 8 标签标识 / 9 注册监管 / 10 随车工具

归集顺序:
1. 优先解析 8 列的「对标维度」文本(支持 `(一)…(十)` 编号与维度名关键词)
2. 否则按 28 列「法规领域」映射:安全→1;节能环保→2;汽车电子→3;**电动汽车→4和5**;**智能网联→6和7**;标识要求→8;入关/上牌/其他/生产一致性要求/维修保养售后→9;照明→1
3. 仍为空时用关键词正则兜底(完整正则见一期源码 `KW` 数组)
- **重要陷阱:≤3 字母的大写缩写(VIN、RED、COC 等)必须加 `\b` 词边界**,否则 VIN 会匹配 moVINg
- 即使前两层已命中,网络安全(6)/ADAS(7)/随车工具(10)的关键词命中仍要**追加**

### 3.3 去重与主键
- 记录主键:`国家 + "|" + norm(法规编号 || 中文名)`,其中 `norm` = 去全部空白 + 转大写
- 同编号不同修订系列视为不同记录(编号中的系列号会进入主键)

### 3.4 国际标准引用提取(关系网络与对账的基础)
从 `法规编号 + 对应欧标 + 对应美标 + 对应国标 + 原文名` 拼接文本中提取:
- `UN R{n}`:正则 `(?:UN[\s-]*R|ECE[\s-]*R?|R)\s*\.?\s*(\d{1,3})(?!\d)`,n 限 1–200
- `GTR {n}`、`FMVSS {n}`、`GB {nnnnn}`(GB 或 GB/T,4–5 位数字)
- 修订系列识别:`(\d{2})\s*(?:series|系列)`、`amd\.?\s*(\d+)`、`amendment\s*(\d+)`、编号中 `\.(\d{2})\b`

### 3.5 覆盖判定(用户的硬指标)
- 单维度:≥3 条 = ✓ 绿;1–2 条 = ⚠ 黄(弱覆盖);0 条 = ✗ 红(空白)
- 单国合格线:**空白维度 ≤ 2 处**,超标即在 KPI「空白超标国家」计数并标红

### 3.6 上位法规解析
- 同国范围内匹配:先按 norm(编号) 精确,再按 norm(中文名) 精确
- 匹配不到的上位文本保留为「上位库外」幽灵标注(蓝标),用作清单缺漏线索
- 网络图中上位→下位为带箭头的橙色边(accent 色),与灰色引用边区分

### 3.7 文本对账匹配(优先级评分,阈值 55)
对每个文本文件(只用文件名,去扩展名后 norm):
- 100:与某条法规 norm(编号) 完全相等
- 90:文件名包含编号(编号长度 ≥4)
- 70:编号包含文件名(文件名长度 ≥6)
- 60:文件名与法规编号各自提取的国际标准引用(3.4 规则)有交集
- 55:文件名包含 norm(中文名)(中文名长度 ≥4)
- 评分 <55 视为不匹配;同分取编号更长者
- 归属国家:文件路径中含国家名则归该国(**按国家名长度降序匹配,防"中国"误吞"中国台湾"**);否则用用户手动指定;再否则全库搜索
- 跳过隐藏文件与 `.json/.DS_Store`
- 对账输出三类:已匹配(含匹配方式)/ 清单缺文本 / 文本不在清单,可导出三 sheet Excel

### 3.8 新增监控(基准对比)
- 基准 = 主键集合 + 主键→简名映射 + 日期,JSON 格式
- 对比:当前键集 − 基准键集 = 新增;基准键集 − 当前键集 = 移除;按国家分组展示
- 二期改造:**基准快照存数据库,每次导入自动生成快照,不再让用户手动导出 JSON**(保留手动导出作为兼容)

### 3.9 UI 设计语言(保留)
- 纸面色系:`--ink:#1a2420 --paper:#f4f1e8 --panel:#fcfbf6 --accent:#c6502a --blue:#3a6b8a`
- 维度十色见 `DIMCOL` 数组;类别四色:框架法规 #D6E4F0 / 技术法规 #E2EFDA / 管理条例 #FFF2CC / 产品标准 #FCE4D6
- 深墨色吸顶顶栏 + 衬线标题;**不引用任何外部 CDN/字体**(内网可用)

## 四、二期架构(已实施)

- 后端:Supabase 项目 `nxiyifglhycvbrccbdhg`(业主确认),迁移见 `supabase/migrations/`
- 前端:根目录 `index.html` + `config.js` + `xlsx.full.min.js`,纯 fetch 调 PostgREST,零框架零 CDN
- 浏览器解析 Excel → 规整化记录 → 批量 upsert 入库(`on_conflict=country,reg_key`);
  导入后自动写 `import_logs` 并生成 `snapshots` 快照
- 法规原文:业主确认**只登记文件名**(`text_files` 表共享),文件本体上传属 M3
- 登录权限:业主确认暂不做(RLS 全放行),后续加固改策略 + Supabase Auth
- 部署:GitHub Pages(`.github/workflows/pages.yml`);内网 nginx 拷三个文件即可

## 五、验收标准

1. 打开网页 ≤2 秒内展示库中现有数据(万级记录量)
2. 上传清单 Excel:自动识别格式、去重入库、生成导入日志与自动快照;重复导入同一文件不产生重复记录
3. 七个标签页 + 全局搜索功能与一期行为一致(以一期 `index.html` 为对照基准逐页核对)
4. 新增监控默认对比"上一次自动快照",也可手选任意历史快照
5. 文本对账:文件名登记自动匹配入库,双向缺口列表与导出 Excel 与一期一致
6. 多人同时访问看到一致数据
7. 移植期间发现一期逻辑疑似 bug 时:先记录、与业主确认,**不要静默修改业务规则**

## 六、二期上线后业主确认的规则扩展(2026-06,追加于一期规则之上)

以下是与业主在线确认并已实装的扩展,**与第三节同等效力**,后续维护不得当作偏差"修复"回退:

1. **清单归属国家**:导入时若文件名含国家名,整份清单归该国(行内「国家」列
   不一致时弹窗确认;多国汇总文件文件名不含国家名,仍按行内列)。
2. **记录删除**:国家详情页「数据管理」卡片,单条勾选/整国删除,双重确认,
   删除后自动生成快照留痕。
3. **草案识别扩展**:状态列之外,编号/各名称/备注含「草案/提案/draft/proposal/
   proposed」(draft 等英文词带 \b 词边界)也认定为草案;总览「草案监控」卡片
   分国家筛选;`regulations.remark` 列存备注参与识别。
4. **对账宽松匹配档**:3.7 评分表追加 85(去符号后编号全等)与 80(去符号后
   文件名包含编号,宽松键 ≥6),排在精确档之后;宽松键 = norm 后去掉全部
   非字母数字汉字字符。
5. **总览排行**:国家名下显示最近入库时间;卡片头部带「上传新的清单」按钮。

6. **层级树「法规分类」模式**:除一期「上位法规」父子树外,新增按《法规分类框架》
   (5→20→51,数据见 index.html `TAX` 常量)自动归类的树,默认模式。归类用
   标准引用匹配(高置信)+ 关键词/节点名/别名表(`TAX_ALIAS`)匹配,未命中进
   「未分类」。属自动建议,不改动一期任何统计口径。
